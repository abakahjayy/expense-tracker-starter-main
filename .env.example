# Base URL of the FullBackendd API this app talks to.
# Local dev: your backend running on its own port (see FullBackendd's PORT/.env).
# Production: the deployed backend's public URL - set this in your host's
# environment variable settings (Vercel/Netlify/etc.), not in a committed file.
VITE_API_URL=http://localhost:7004
